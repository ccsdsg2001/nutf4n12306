server:
  port: 8001
  servlet:
    context-path: /member

spring:
  datasource:
    url: jdbc:mysql://140.210.221.183:3306/train_member?characterEncoding=UTF8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: member

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

logging:
  level:
    com:
      example:
        mapper: trace
